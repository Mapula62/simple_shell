#include "shell.h"
#include <stdio.h>
/**
 * main - program that passes betty
 * Return: Always 0 (Success)
 */
int main(void);
{
int g;
int m;
printf("g (team project by Gift): %d\n", g);
printf("m (team project by Mapula): %d", m);
return (0);
}
